{
  "name": "demo05",
  "version": "1.0.0",
  "description": "在 Express 可以呈现模板文件之前，必须设置以下应用程序设置：\r * views：模板文件所在目录。例如：app.set('views', './views')\r * view engine：要使用的模板引擎。例如：app.set('view engine', 'jade')",
  "main": "hello.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "popzhou",
  "license": "ISC",
  "dependencies": {
    "express": "^4.15.3",
    "pug": "^2.0.0-rc.2"
  }
}
